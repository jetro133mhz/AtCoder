#include <bits/stdc++.h>

using namespace std;

int main() {
  //配列
  //vector<int> vec(n, 初期値);
  //vector<vector<int>> vec(n, vector<int>(m, 初期値));
  //小数の桁数の出力指定
  //count<<fixed<<setprecision(10);
}